name: Moku Moku

on:
  push:
    branches: [ master ]
#  schedule:
#    - cron: '0 0 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Post Message
      run: 'curl -H "Content-Type: application/json" -X POST -d "{\"username\": \"【定例】もくもく会\", \"content\": \"もくもく会の時間になりました\\n参加する人は以下のフォーマットで宣言を行って下さい。\\n\`\`\`【開始宣言】〇〇\n【終了宣言】〇〇\`\`\`\n\"}" ${{ secrets.DISCORD_WEBHOOK_URL }}'
